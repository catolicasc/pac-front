const Button = ({title, action}) => {

    return <button style={{
        background: "#7C3AED",
        color: "#fff",
        padding: 15, borderRadius: 4
    }}>
        {title}
    </button>
}

export default Button
