const Title = ({title}) => {

    return <>
        <h1 style={{
            fontSize: '40px',
            fontWeight: 700
        }}>
            {title}
        </h1>
    </>
}

export default Title
